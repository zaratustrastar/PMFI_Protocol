# Check if cron trigger is still registered
grep "registerCronWorkflow" src/mastra/index.ts
function addReferralCode(url: string): string {
  const referralCode = "q2XDjZW";
  
  // Check if URL already has query parameters
  if (url.includes("?")) {
    return `${url}&via=${referralCode}`;
--
        const referralUrl = addReferralCode(market.url);
        
        const telegramMessage = `🔮 <b>New Polymarket Market!</b>

<b>Question:</b> ${escapedQuestion}

import { inngest, inngestServe, registerCronWorkflow } from "./inngest";
registerCronWorkflow("* * * * *", polymarketWorkflow);
root@ubuntu-s-1vcpu-1gb-sfo3-01:/opt/polymarket-bot# # Check if Inngest sees any apps/functions registered
journalctl -u inngest.service --since "22:47:00" --no-pager | grep -i "app\|function\|sync"
Nov 24 22:47:10 ubuntu-s-1vcpu-1gb-sfo3-01 bash[62097]: {"time":"2025-11-24T22:47:10.991797329Z","level":"INFO","msg":"subscribing to function queue"}
Nov 24 22:47:10 ubuntu-s-1vcpu-1gb-sfo3-01 bash[62097]: {"time":"2025-11-24T22:47:10.995441565Z","level":"INFO","msg":"apps synced, disabling auto-discovery"}
Nov 24 22:49:06 ubuntu-s-1vcpu-1gb-sfo3-01 bash[62187]: {"time":"2025-11-24T22:49:06.227916735Z","level":"INFO","msg":"subscribing to function queue"}
Nov 24 22:49:06 ubuntu-s-1vcpu-1gb-sfo3-01 bash[62187]: {"time":"2025-11-24T22:49:06.232479034Z","level":"INFO","msg":"apps synced, disabling auto-discovery"}
root@ubuntu-s-1vcpu-1gb-sfo3-01:/opt/polymarket-bot# cd /opt/polymarket-bot

# See what the last commit was (should be my referral changes)
git log --oneline -3

# Revert to the commit BEFORE my changes
git reset --hard HEAD~1

# Restart services
systemctl restart inngest.service
sleep 5
systemctl restart mastra.service
sleep 10

# Check if workflow starts running again
journalctl -u mastra.service --since "1 minute ago" --no-pager | grep "monitorAndPost"
63c370e (HEAD -> main, origin/main, origin/HEAD) Saved progress at the end of the loop
ad75e81 Add referral code to all market links sent to Telegram
70f1fe9 Add referral link to all Polymarket market notifications
HEAD is now at ad75e81 Add referral code to all market links sent to Telegram

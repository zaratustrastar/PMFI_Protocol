Here’s a task you can literally paste into Replit so it doesn’t start “optimizing” random stuff:

---

**Task for Replit (paste this as-is):**

I have a TypeScript Telegram bot workflow in:

`src/mastra/workflows/polymarketWorkflow.ts`

Right now, my Telegram message text includes **hard-coded hashtags** like:

```ts
// Example (exact string may differ)
const telegramMessage = `
🔮 <b>New Polymarket Market!</b>

<b>Question:</b> ${escapedQuestion}

<b>Market:</b> ${safeUrl}

#Polymarket #PredictionMarkets
`;
```

I want to **remove those static hashtags** and instead generate **category-based hashtags** from each market.

Please do the following, and only touch this workflow file and any small helpers it needs:

1. **Remove the static hashtags** `#Polymarket #PredictionMarkets` from the message template.

2. **Add a helper** to derive the primary category from the market object. Use this logic:

   * If `market.categories` is a non-empty array → use `market.categories[0]`.
   * Else if `market.slug` is a string containing `/` → use the part before the first `/`.
   * Else → use `"other"` as a fallback.

3. **Normalize the category into a hashtag**:

   * Lowercase the raw category string.
   * Map some common categories to nice hashtag forms, for example:

     * `"sports"` → `#Sports`
     * `"politics"` → `#Politics`
     * `"crypto"` → `#Crypto`
     * `"finance"` → `#Finance`
     * `"news"` → `#News`
     * `"other"` or unknown → `#Markets`
   * Make sure the hashtag has **no spaces or special chars** (only letters and numbers after `#`).

   A helper like this is fine:

   ```ts
   function getMarketCategory(market: any): string { ... }

   function getCategoryHashtag(category: string): string {
     const c = category.toLowerCase();
     if (c === "sports") return "#Sports";
     if (c === "politics") return "#Politics";
     if (c === "crypto") return "#Crypto";
     if (c === "finance") return "#Finance";
     if (c === "news") return "#News";
     return "#Markets";
   }
   ```

4. **Use the category hashtag in the Telegram message**:

   * Inside the workflow, after you have `market`, call:

     ```ts
     const category = getMarketCategory(market);
     const categoryHashtag = getCategoryHashtag(category);
     ```

   * Append `categoryHashtag` to the message body (for example in the footer), e.g.:

     ```ts
     const telegramMessage = `
     ```

🔮 <b>New Polymarket Market!</b>

<b>Category:</b> ${category} <b>Question:</b> ${escapedQuestion}

<b>Market:</b> ${safeUrl}

${categoryHashtag}
`.trim();
```

5. **Very important:**

   * Do **not** change any cron registration, Inngest config, or systemd-related code.
   * Do **not** add new dependencies.
   * Keep everything TypeScript-safe and non-breaking; helpers should be defensive (no runtime errors if `category` is missing).

Only implement the above changes. Do not refactor unrelated parts of the project.

---

You can tweak the hashtag mapping in point 3 if you want different names, but this prompt should keep Replit pointed *exactly* where you want it.
